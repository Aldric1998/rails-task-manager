<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "Edit_Task", edit_task_path(@task) %>
<p>-------</p>
<%# <%= link_to "Delete_Task", delete_task_path%>
<%= link_to "Delete #{@task.title}",
            delete_task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<p>-------</p>
<%= link_to "Task", tasks_path%>
